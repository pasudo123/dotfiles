# Agents.md

## 작업 원칙
- 모든 운영 규칙은 이 `Agents.md`에서 관리한다.
- 직접 `main`에서 작업하지 않는다. 항상 작업 브랜치를 만든다.
- 변경 후 PR을 만들고, PR 링크를 공유한다.
- PR 머지 완료 후:
  1. 로컬 브랜치 삭제
  2. 리모트 브랜치 삭제
  3. `main`으로 복귀

## 기본 작업 흐름
1. `main` 최신화
2. 작업 브랜치 생성
3. 변경/검증
4. 커밋/푸시
5. PR 생성 및 링크 공유
6. 리뷰/머지
7. 브랜치 정리 및 `main` 복귀

## PR 작성 규칙 (짧고 굵게)
- 길게 쓰지 않는다. 핵심만 3~5줄로 작성한다.
- "무엇을", "왜", "어떻게 확인했는지"만 쓴다.
- 스크린샷/로그는 필요한 경우만 첨부한다.
- PR 본문에서 `##` 같은 Markdown 헤딩은 사용하지 않는다.
  (GitHub에서 글씨가 크게 보여 가독성이 떨어질 수 있음)
- 섹션 구분은 `**요약**`, `**이유**`, `**검증**`, `**참고**` 형태의
  굵은 라벨 + 불릿으로 작성한다.
- `gh pr create` 시 본문은 `--body-file` 사용을 기본으로 한다.
  (`--body` 인라인 문자열은 줄바꿈/기호가 깨지기 쉬움)

## PR 템플릿
```md
**요약**
- {무엇을 변경했는지 1~2줄}

**이유**
- {왜 필요한지 1~2줄}

**검증**
- {실행한 검증 2~4개}

**참고**
- {영향 범위/주의사항, 없으면 생략}
```

## PR 생성 예시
```bash
# 1) 템플릿 파일 작성
cat > /tmp/pr-body.md <<'EOF'
**요약**
- ...

**이유**
- ...

**검증**
- ...
EOF

# 2) PR 생성
gh pr create --base main --head <branch> --title "<title>" --body-file /tmp/pr-body.md
```
